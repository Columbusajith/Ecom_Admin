specVersion: "v1beta"
serviceId: "ecomadmin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ecomadmin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
